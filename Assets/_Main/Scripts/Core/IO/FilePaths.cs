using UnityEngine;

public class FilePaths
{
    //this is the Asset folder within Unity or changes to the AssetFiles folder if I builded the project
    public static readonly string ROOT = $"{Application.dataPath}/gameData/";
}
